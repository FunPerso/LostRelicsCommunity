import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flexDirection: "row",
    margin: 16,
    borderWidth: 1,
    borderColor: "black"
  },
  text: {
    color: "white",
    fontSize: 17,
    fontWeight: "bold",
    marginBottom: 8
  }
});
